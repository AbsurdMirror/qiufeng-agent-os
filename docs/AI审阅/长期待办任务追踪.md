# 长期待测与待实现任务追踪

## 待测试任务 (Pending Tests)
* [ ] **飞书 Webhook 模式连通性测试**：目前仅测试了长连接模式。由于本地开发环境缺乏公网 IP，Webhook 模式（基于 `verify_token` 和 `encrypt_key` 的验签与解密）尚未进行真实环境端到端验证。需在后续有公网映射（如 ngrok）或测试服部署时补充验证。

## 待实现任务 (Pending Implementations)
* [ ] **全量交互式配置 CLI**：基于终端交互库（如 `questionary` 或 `InquirerPy`），结合 `rich` 库，实现一个菜单式的系统配置引导程序，并支持将所有配置序列化到统一的本地配置文件中。
* [ ] **飞书 API 客户端升级**：随着业务深入，如果运行期需要高频调用飞书 API（如发送富文本消息、拉取文档），建议将目前基于 `urllib` 的轻量级实现重构，引入 `aiohttp` 或飞书官方 `lark-oapi` 库，以支持连接池复用和更复杂的异步请求。
* [ ] **补齐第三方库的类型存根 (Type Stubs)**：当前依赖的 `nonebot2` 和 `lark_oapi` 缺少官方的 py.typed 标记和类型存根文件，导致 `mypy` 无法进行深度静态分析。未来在进行严格代码审计前，需考虑自行编写关键接口的 `.pyi` 文件，或等待官方更新。
* [ ] **InMemoryStore 线程安全与 OOM 防范**：`src/storage_memory/contracts.py` 中的 `InMemoryHotMemoryStore` 缺失锁机制，在多线程环境（如多线程 Webhook 处理请求）下直接操作字典会导致竞态条件。此外，`ltrim` 方法的边界条件（当 `stop == -1` 且列表为空时）可能无法正确收敛，且未对内存容量做全局限制，高频请求下存在内存泄漏（OOM）风险。
* [ ] **Lambda 类型推断修复**：在 `src/storage_memory/bootstrap.py` 等模块中使用了 `lambda` 闭包，导致静态检查工具 `mypy` 无法推断其复杂签名（提示 `Cannot infer type of lambda`）。未来应重构为具名包装函数。
* [ ] **请求染色判定机制优化**：目前 `src/observability_hub/request_coloring.py` 中的判定逻辑存在两个待优化点：1. 状态内聚性不足，调用 `is_request_colored` 必须主动传入 `state`，未来应考虑结合 `contextvars` 隐式传递；2. 提取逻辑风险，`_read_value` 目前仅支持字典浅层键值判断，如果上游传入深度嵌套的上下文（如 `{"header": {"traceId": "..."}}`），会导致漏判，需引入类似 JSONPath 的深层提取机制。
* [ ] **编排器返回值强类型重构**：`src/orchestration_engine/base_orchestrator.py` 中的 `BaseOrchestrator.execute` 方法目前返回 `dict[str, object]`，这种返回值类型约束比较弱，虽然提供了灵活性，但也丢失了强类型带来的安全感。后续编排引擎执行结果如果结构固定，应考虑抽象为专门的强类型数据类。
* [ ] **模型请求状态透传的深拷贝保护**：在 `src/model_provider/litellm_adapter.py` 的 `build_litellm_completion_payload` 方法中，处理 `metadata` 时仅使用了浅拷贝（`dict(request.metadata)`）。如果上层传递了嵌套层级较深的可变对象（如列表、字典），在后续修改或异步透传时可能会篡改原始请求数据，需引入深拷贝机制保障状态隔离。
* [ ] **全局异常捕获与隔离兜底**：在 `src/skill_hub/capability_hub.py` 的 `RegisteredCapabilityHub.invoke` 方法中，执行 `await handler(request)` 时缺少 `try...except` 块包裹。如果下游工具抛出未处理的异常，会导致调用链崩溃，破坏编排层的并发稳定性，需补充统一的能力调用异常拦截机制。
* [ ] **反向注册机制重构**：`ModelCapabilityRouter` 向 `RegisteredCapabilityHub` 注册时需要显式调用 `register_into` 传入容器本身（反向依赖）。后续如果扩展出更多复杂能力组，会导致注册代码变冗长。可以考虑引入依赖注入框架或使用基于装饰器的自动发现机制。
* [ ] **[REV-002] 锁内全量遍历导致的性能雪崩缺陷**：在 `SessionContextController` 的 `is_duplicate` 中，每次新消息到来都在对象锁内全量遍历字典的 `items()`，在极高并发或积压时会迅速导致网关处理线程排队并大幅降低吞吐率。建议引入异步清理任务或专用的时间轮算法结构。
* [ ] **[REV-009] 粗暴的数据强转大乱炖重构**：在 `channel_gateway/feishu_long_connection.py` 尾部的 `_to_mapping` 用了 `hasattr` 拼凑去反射不标准的数据类。若未来对接更复杂的异构组件，容易陷入递归死锁与栈溢出断崖。建议寻找官方事件结构的 `json()` 原生转储方法或使用 `pydantic` 做数据防腐与洗牌。
* [ ] **[REV-013] 写死在肚子里的“刻舟求剑”字典与魔法值 (Hardcoded Configs)**：在 `model_provider/router.py` 中的 `_context_windows` 字典直接把 4096 / 8192 等不同厂商模型能装几句话的上下文长度生吞硬剥写烂在了初始化函数里（顺带 `StateContextManager` 也有个对应的极其相似的硬写 `limit=10` 的魔数）。这类高频随着各厂商政策放宽或锁死的数据，未来必定会导致代码常被打上布丁。其必须被剥离重构成由项目外界统一下发的专门配置装配结构。
* [ ] **[REV-029] 企业级架构重构：根绝隐式灾难退化 (Strict Fail-Fast on Infrastructure Storage)**：针对目前在 `storage_memory/bootstrap.py` 中“若 Redis 连不上则强制降维换用内存存储 `InMemoryStore`”的 P0 补丁设计手段，虽然这在单体框架开发中保障了开箱即死用，但在微服务与云原生集群环境下，它是不可接受的。一旦云端 Redis 遭遇波动，造成单个新拉起的节点因探活失败而自动偷偷降级成自己本地的内存版，会导致集群中出现严重失忆、状态不一致与“容器脑裂”。日后系统需架构重构为：一旦用户在配置项显式驱动了并指定 `redis` 为主存储，点火时任何一丁点连不上的状况都必须直接向控制台抛出 `Critical Error`且毫不留情地阻断程序，实现“宁愿直接报错死掉，不拉残血上去丢人”的高标准企业级（Fail-Fast）规范。
* [ ] **[REV-OBEXPORTS-BUG-001] 接口层依赖污染重构：实现类导入泄露**：目前项目内各模块的 `exports.py` 文件（如 `src/observability_hub/exports.py`）存在直接从实现模块（如 `jsonl_storage.py`）导入底层具体类的现象。这导致任何 import 导出容器的消费方都会强制触发底层副作用导入，破坏了“接口与实现分离”的原则，并可能引发循环依赖。全局范围内的 `exports.py` 需重构为：使用 `TYPE_CHECKING` 进行类型声明，并在运行时采用 `Protocol` 协议定义或字符串延迟引用，确保导出容器在运行时是轻量级且无依赖污染的。
* [ ] **[REV-FEI-RISK-001] 并发海啸下的 Token 击穿与重复刷新（飞书网关）**：`src/channel_gateway/feishu_sender.py` 的 Token 获取逻辑（`_get_tenant_access_token`）缺乏并发控制机制。在异步协程高频运转时，如果 Token 恰好过期（或初始未缓存），大量并发协程会同时判定为失效，从而一窝蜂地向飞书开放平台发起重复的授权请求，导致网络浪费与潜在封禁限流风险。后续需引入 `asyncio.Lock()` 实现防击穿机制，确保只有一只领头羊执行刷新。
* [ ] **[REV-CAP-CON-001] 脆弱的魔数规避与枚举重构（能力中心）**：`src/skill_hub/capability_hub.py` 在 `register_capability` 兜底领域沙盒时使用了 `if capability.domain == "tool":`。这种散落在关键判别逻辑中的硬编码魔法字符串在未来扩展时极易遭到人为拼写失误（如误写为 "tools" 等）且无法在编译阶段报警，进而导致安全防线静默失效。需将其重构为官方 `Enum` 或 `typing.Literal` 常量引用强制约束。
* [ ] **[REV-JSONL-CON-001] 物理磁盘 IO 导致的异步并发管线大塞车（观测中心）**：目前的 `jsonl_storage.py` 在执行 `write_record` 落地时，直接使用了互斥锁包含的阻塞式 `open("a")` 同步写入。由于系统底座是纯 Asyncio 异步架构，在面对百万级高频并发网络流量时，任意一个协程陷入几十微秒的物理慢磁盘 IO 都会瞬间霸占整个事件循环，导致其他协程排起长龙而让整个大模型调度网关死锁卡顿。后续极其有必要引入 “内存并发队列（`queue.Queue`）无锁丢入 + 单一后台专属守护线程（Consumer Worker）按批次解包刷写硬盘” 的双结构落盘方案，将前端的主线程耗时降到纳秒级别。
* [ ] **[REV-RESP-CON-001] 移除字符上限后的无限容量溢出风险（响应原语）**：`src/channel_gateway/responses.py` 中移除了 `ReplyText.content` 的 4000 字符限制后，该字段理论上可以接收无限大的字符串。如果上游大模型输出失控陷入死循环，可能会导致内存中积压超大字符串，最终引发 OOM（内存溢出）。建议在 `__post_init__` 中添加一个极其宽松的物理极限兜底（例如 `1,000,000` 字符），在不影响正常长文本分发的前提下提供最后一道防线。
* [ ] **[REV-FEI-CON-001] 暴力切分导致 Markdown 排版错乱风险（飞书网关）**：`src/channel_gateway/feishu_sender.py` 的切片方案采用纯字数硬切。如果切开点正好在 Markdown 代码块（`` ``` ``）或加粗（`**`）语法中间，会导致飞书端的渲染完全崩溃。建议引入智能断点寻找逻辑，优先在换行符或语义完整处进行切分。
* [ ] **[REV-FEI-RISK-002] 分片发送中途失败导致内容残缺（飞书网关）**：`src/channel_gateway/feishu_sender.py` 在循环发送切片时，一旦中间某个请求失败则直接中断退出，且用户无法感知内容已丢失。建议增加重试机制，或者在失败时发送一条提示消息告知用户“后续内容发送失败”。
* [ ] **[REV-VAL-RISK-002] 脆弱的异常字符串匹配兜底（模型验证层）**：`src/model_provider/schema_validator.py` 目前通过 `if any(keyword in err_str for keyword in ["authentication", "quota", ...])` 来拦截不可自愈的致命异常。不同大模型厂商的报错信息千差万别，仅靠英文单词硬匹配极易导致“误杀”或“漏杀”。未来应在 Provider 适配层抽象出统一的 `FatalProviderError` 并通过异常类本身进行精准拦截。